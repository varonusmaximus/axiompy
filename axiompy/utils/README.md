# `axiompy.utils`

Reserved package for **small shared helpers** (date/string utilities, etc.). Modules here are intentionally thin; prefer putting domain logic in the appropriate `axiompy` subpackage or sibling distribution.

When you add real implementations, document them in this README and add tests under `tests/`.
